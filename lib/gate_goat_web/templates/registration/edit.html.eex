<h2>Edit Registration</h2>

<%= render "edit_form.html", Map.put(assigns, :action, Routes.registration_path(@conn, :update, @registration)) %>

<span><%= link "Back", to: Routes.lookup_path(@conn, :lookup, %{"search" => %{"confirmation_number" => @registration.id}}) %></span>
