<h2>New Registration</h2>

<%= render "form.html", Map.put(assigns, :action, registration_path(@conn, :create, event_id: @event_id)) %>

<span><%= link "Back", to: registration_path(@conn, :index) %></span>
